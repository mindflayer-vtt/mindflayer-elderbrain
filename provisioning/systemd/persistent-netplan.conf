[Service]
# The boot generator may have read the OS mountpoint before persistent /etc
# configuration became available. Regenerate from mounted user settings before
# network recovery and network-pre.target; do not activate interfaces here.
ExecStartPre=/usr/bin/env netplan generate
