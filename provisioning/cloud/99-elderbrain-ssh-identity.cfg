#cloud-config
# openssh-server creates fresh keys during installation. Preserve installs bind
# the existing appliance identity before first boot; cloud-init must not replace
# those keys through /etc/ssh. Provisioning validates the final keys with sshd -t.
ssh_deletekeys: false
